specVersion: "v1"
serviceId: "swapshelf"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "swapshelf-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
